# Simple_Messenger
JAVA MCA I Project

This is the first protoype

NOTE:- Make sure internet is setup at build-compile time and ther server, clients are on same tcp/ip network.

✅Stept to run :--

A> Install jdk-25.0.1, the project supports jdk-23-25

B> run gradlew.bat(windows) or gralde(linux,mac)

C> Now run "./gradlew build" and run "./gradlew runServer" on server machine. 

D> run "./gradlew runClient" in separate window or machine and insert the ip of server(machine running runServer)

NOTE: If having difficulties, maybe you need to install gradle 9.0 too.
 
