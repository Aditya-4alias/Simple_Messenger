# Simple_Messenger
JAVA MCA I Project

This is the first protoype

NOTE:- Make sure internet is setup

✅Stept to run :--

A> Install jdk-25.0.1, javafx25
B> run gradlew.bat(windows) or gralde(linux,mac)
C> Now run "./gradlew runServer" and "./gradlew runClient" in separate windows and insert the ip of server(machine running runServer).
D> keep javafx in same folder as Simple_Messenger project
E> you may need to recreate gradle wrapper. again search online to run project

NOTE: If having difficulties search online maybe you need to install gradle 9.0 too.
